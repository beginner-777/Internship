# Production screenshots

After the Vercel deployment is live, capture and add:

1. `home.png` — the portfolio command center at desktop width.
2. `assistant.png` — the open Gemini assistant showing a successful verified answer.
3. `mobile.png` — the command center or assistant at approximately 375 px width.

Update the main README screenshot table with these relative paths before final submission.

